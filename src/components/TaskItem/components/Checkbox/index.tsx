import { TouchableOpacity, View } from "react-native";
import { CheckboxContainer, CheckboxStyled } from "./styles";

interface CheckboxProps {}

export function Checkbox() {
  return (
    <CheckboxContainer>
      <CheckboxStyled></CheckboxStyled>
    </CheckboxContainer>
  );
}
